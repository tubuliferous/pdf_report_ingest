Ingest PDF reports from directory (including all subdirectories) and return summary TSV document. 

Usage: 

  ./report_ingest.py -d pdfs_directory -o output.tsv

or:

  ./report_ingest.py --dir pdfs_directory --out output.tsv
